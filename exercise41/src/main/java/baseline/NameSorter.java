package baseline;

import java.io.File;
import java.util.List;

public class NameSorter {

    //Create private final inputFile

    NameSorter(File inputFile) {
        //this inputFile = inputFile
    }

    public List<String> createListOfNames() {
        //Create a list of strings "nameList"
        //Try to read in inputFile with a BufferedReader "reader"
            //String line = reader.readLine()
            //while line is not null:
                //Add line to nameList
                //line = reader.readLine()
        //return nameList
        return null;
    }

    public List<String> sortNames(List<String> names) {
        //Sorts the names in the list using the Collections.sort method
        //return names
        return null;
    }

    public String listOutput(List<String> names) {
        //Create new StringBuilder "output"
        //Append "Total of 'names.size()' names%n" to output
        //Append "-----------------" to output
        //For each name in names:
            //Append "%n'name'" to output.
        //return string value of output
        return null;
    }
}
